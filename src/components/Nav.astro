---
import { navLinks, profile } from "@/lib/profile";
import ThemeToggle from "./ThemeToggle.astro";

const firstName = profile.name.split(" ")[0].toLowerCase();
---

<header
  id="main-nav-header"
  class="fixed inset-x-0 top-0 z-50 border-b border-transparent transition-colors"
>
  <nav class="mx-auto flex h-16 w-full max-w-5xl items-center justify-between px-6">
    <a
      href="/#inicio"
      class="font-mono text-sm font-medium tracking-tight"
    >
      <span class="text-accent">&lt;</span>
      {firstName}
      <span class="text-accent">/&gt;</span>
    </a>

    <ul class="hidden items-center gap-8 md:flex">
      {navLinks.map((link) => (
        <li>
          <a
            href={link.href}
            class="text-sm text-muted transition-colors hover:text-foreground"
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>

    <div class="flex items-center gap-3">
      <ThemeToggle />
      <button
        id="mobile-nav-toggle"
        type="button"
        class="inline-flex h-9 w-9 items-center justify-center rounded-full border border-border text-muted transition-colors hover:border-accent hover:text-accent md:hidden"
        aria-label="Abrir menú"
        aria-expanded="false"
        aria-controls="mobile-nav-menu"
      >
        <svg
          id="menu-open-icon"
          class="h-4 w-4"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          aria-hidden="true"
        >
          <path d="M4 7h16M4 12h16M4 17h16"></path>
        </svg>
        <svg
          id="menu-close-icon"
          class="hidden h-4 w-4"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          aria-hidden="true"
        >
          <path d="M18 6 6 18M6 6l12 12"></path>
        </svg>
      </button>
    </div>
  </nav>

  <ul
    id="mobile-nav-menu"
    class="hidden border-t border-border px-6 py-4 md:hidden bg-background/95 backdrop-blur-md"
  >
    {navLinks.map((link) => (
      <li>
        <a
          href={link.href}
          data-mobile-nav-link
          class="block py-3 text-base text-muted transition-colors hover:text-foreground"
        >
          {link.label}
        </a>
      </li>
    ))}
  </ul>
</header>

<script>
  function initNav() {
    const header = document.getElementById("main-nav-header");
    const toggleBtn = document.getElementById("mobile-nav-toggle");
    const menu = document.getElementById("mobile-nav-menu");
    const openIcon = document.getElementById("menu-open-icon");
    const closeIcon = document.getElementById("menu-close-icon");
    const links = document.querySelectorAll("[data-mobile-nav-link]");

    if (!header || !toggleBtn || !menu) return;

    let isOpen = false;

    const updateHeaderStyle = () => {
      const isScrolled = window.scrollY > 8;
      if (isScrolled || isOpen) {
        header.classList.add("border-border", "bg-background/80", "backdrop-blur-md");
        header.classList.remove("border-transparent");
      } else {
        header.classList.remove("border-border", "bg-background/80", "backdrop-blur-md");
        header.classList.add("border-transparent");
      }
    };

    const toggleMenu = (forceState?: boolean) => {
      isOpen = typeof forceState === "boolean" ? forceState : !isOpen;
      menu.classList.toggle("hidden", !isOpen);
      if (openIcon && closeIcon) {
        openIcon.classList.toggle("hidden", isOpen);
        closeIcon.classList.toggle("hidden", !isOpen);
      }
      toggleBtn.setAttribute("aria-expanded", isOpen.toString());
      toggleBtn.setAttribute("aria-label", isOpen ? "Cerrar menú" : "Abrir menú");
      updateHeaderStyle();
    };

    toggleBtn.addEventListener("click", () => toggleMenu());
    links.forEach((l) => l.addEventListener("click", () => toggleMenu(false)));
    window.addEventListener("scroll", updateHeaderStyle, { passive: true });
    updateHeaderStyle();
  }

  initNav();
  document.addEventListener("astro:after-swap", initNav);
  document.addEventListener("astro:page-load", initNav);
</script>
